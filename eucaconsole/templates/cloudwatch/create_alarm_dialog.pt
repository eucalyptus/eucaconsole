<div class="modal create-alarm-modal" modal="createAlarm">
    <div create-alarm=""
        template="${request.route_path('render_template', subpath='cloudwatch/create_alarm_modal_template')}"
        resource-type="${resource_type}"
        resource-id="${resource_type_value}"
        resource-name="${resource_name}"
        default-metric="${default_metric}"
        default-statistic="${default_statistic}"></div>
</div>
