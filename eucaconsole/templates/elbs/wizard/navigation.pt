<dl class="tabs">
    <dd ng-repeat="step in nav.validSteps() track by $index" ng-class="nav.status(step)">
        <a ng-click="nav.visitStep(step)">
            <span class="cir">{{ $index + 1 }}</span>
            {{ step.label }}
        </a>
    </dd>
</dl>
